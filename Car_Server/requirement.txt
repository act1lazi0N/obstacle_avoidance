# =============================================================
# Thư viện cần thiết cho Car_Server (Local Server / Cloud)
# Bao gồm: ai_controller.py, collect_data.py
# =============================================================
# Cài đặt:
#   pip install -r requirement.txt
# =============================================================

# --- Web Server (GUI) ---
flask                     # Chạy web server cho GUI điều khiển
flask-cors                # Hỗ trợ Cross-Origin (nếu cần)

# --- Computer Vision ---
opencv-python             # Xử lý ảnh + hiển thị GUI (cv2.imshow)

# --- Networking ---
requests                  # Gửi HTTP request đến Raspberry Pi

# --- AI / Deep Learning ---
torch                     # PyTorch – framework chạy model YOLOv5
torchvision               # Xử lý ảnh cho PyTorch (transforms, models)
ultralytics               # YOLOv5/v8 – thư viện phát hiện vật thể

# --- Data Processing ---
numpy                     # Tính toán mảng số (xử lý ảnh, cảm biến)
pandas                    # Đọc kết quả detection dạng DataFrame

# --- Configuration ---
python-dotenv             # Đọc biến môi trường từ file .env
